# Dépendances principales
opencv-python-headless>=4.5
numpy>=1.21

# Backend NCNN (optionnel — fallback sur OpenCV DNN si absent)
# ncnn

# Pour Raspberry Pi : utiliser opencv-python-headless pour éviter les dépendances GTK
# Sur Pi avec écran : remplacer par opencv-python si besoin de cv2.imshow()
# pip install opencv-python

# Optionnel : Picamera2 (caméra native Pi via libcamera)
# pip install picamera2
